import { CurrencyMaskConfig } from 'ngx-currency/src/currency-mask.config';

export const CURRENCY_MASK_CONFIG_PTBR: CurrencyMaskConfig = {
    align: "right",
    allowNegative: true,
    allowZero: true,
    decimal: ",",
    precision: 2,
    prefix: "R$ ",
    suffix: "",
    thousands: ".",
    nullable: true
  };
  
